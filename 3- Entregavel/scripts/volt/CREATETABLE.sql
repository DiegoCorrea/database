CREATE TABLE embrapinha (
mes VARCHAR(3),
temperatura FLOAT,
pluviometrico INTEGER,
evaPotencial INTEGER,
armazenHidrico INTEGER,
evaReal INTEGER,
defHidrica INTEGER,
excedHidrico INTEGER,
cidade VARCHAR(32)
);
CREATE TABLE embrapadez (
mes VARCHAR(3),
temperatura FLOAT,
pluviometrico INTEGER,
evaPotencial INTEGER,
armazenHidrico INTEGER,
evaReal INTEGER,
defHidrica INTEGER,
excedHidrico INTEGER,
cidade VARCHAR(32)
);
CREATE TABLE embrapacem (
mes VARCHAR(3),
temperatura FLOAT,
pluviometrico INTEGER,
evaPotencial INTEGER,
armazenHidrico INTEGER,
evaReal INTEGER,
defHidrica INTEGER,
excedHidrico INTEGER,
cidade VARCHAR(32)
);
CREATE TABLE embrapamil (
mes VARCHAR(3),
temperatura FLOAT,
pluviometrico INTEGER,
evaPotencial INTEGER,
armazenHidrico INTEGER,
evaReal INTEGER,
defHidrica INTEGER,
excedHidrico INTEGER,
cidade VARCHAR(32)
);
