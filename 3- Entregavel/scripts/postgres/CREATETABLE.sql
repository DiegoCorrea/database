CREATE TABLE embrapa (
mes VARCHAR(3),
temperatura FLOAT,
pluviometrico INTEGER,
evaPotencial INTEGER,
armazenHidrico INTEGER,
evaReal INTEGER,
defHidrica INTEGER,
excedHidrico INTEGER,
cidade VARCHAR(32)
);
CREATE TABLE embrapa2 (
mes VARCHAR(3),
temperatura FLOAT,
pluviometrico INTEGER,
evaPotencial INTEGER,
armazenHidrico INTEGER,
evaReal INTEGER,
defHidrica INTEGER,
excedHidrico INTEGER,
cidade VARCHAR(32)
);
CREATE TABLE embrapa3 (
mes VARCHAR(3),
temperatura FLOAT,
pluviometrico INTEGER,
evaPotencial INTEGER,
armazenHidrico INTEGER,
evaReal INTEGER,
defHidrica INTEGER,
excedHidrico INTEGER,
cidade VARCHAR(32)
);
CREATE TABLE embrapinha (
mes VARCHAR(3),
temperatura FLOAT,
pluviometrico INTEGER,
evaPotencial INTEGER,
armazenHidrico INTEGER,
evaReal INTEGER,
defHidrica INTEGER,
excedHidrico INTEGER,
cidade VARCHAR(32)
);